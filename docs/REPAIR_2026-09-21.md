# 交接问题修复记录

> 历史记录：当前实现、验收和使用步骤以 [REPAIR_2026-09-22.md](REPAIR_2026-09-22.md) 为准。下文未接入运行时等描述属于此前阶段。

本轮保留 metadata-only、loopback-only、fail-closed 边界，没有运行样本、连接 MCP 或安装工具。

## 已实施

- 移除初始虚构任务、风险评分、趋势图、活动、聊天及知识库步数；加载后只展示 API 记录。风险无分析依据时显示未评估。
- 工作区选择、完整 Case 表单、样本库列表、选择和拖放文件登记、元数据检查、任务取消、人工证据登记。
- 证据状态筛选、每页 20 条、草稿保留、提交防重、已复核备注和审计反馈；数字及进度条共用复核比例。
- 证据图使用 Case 内样本、任务、证据及显式引用；节点拖动、背景平移、缩放、复位、适应画布、查看记录。不同 Case 的布局保存在独立内存对象中。
- 中央、左栏、聊天独立滚动；窄屏有 Copilot 展开入口；复核输入适配深浅主题。
- 活动和通知入口显示持久化审计事件；实际同步时间、离线状态和轮询重连；聊天按 Case 加载已保存记录。
- 展示实际报告列表、格式、时间和 SHA-256；下载处理旧报告无快照及校验错误。
- Playbook 新建、编辑、删除和持久化，步骤数来自定义，文本不执行。
- 集成配置 JSON 编辑页：模板、版本、修订冲突、持久化和严格 schema 校验；Provider 协议/Base URL/模型/凭据引用草案、MCP、Skill、worker、子 Agent 预算定义。
- 凭据只接收环境变量名，拒绝额外密钥字段和带认证信息 URL；配置不进入证据报告。恢复模板前显示准确替换范围。
- README 修正会话令牌和字段校验描述；任务增加来源、操作者、执行器和起止时间，演示属性向派生记录传播。

## 按钮清单

| 入口 | 行为 |
|---|---|
| 导航、样本库跳转、用户菜单 | 打开对应页面 / 集成配置 |
| 工作区选择 | 切换 Case 并刷新全部上下文 |
| 新建分析 | 填写名称和授权说明，创建 Case |
| 导入、选择文件、拖放 | 浏览器计算哈希，仅提交元数据 |
| 运行 AI、PE/APK 工作流 | 登记 blocked 任务，显示未执行原因 |
| 元数据检查、取消任务 | 调用真实 API 并显示结果 |
| 通知、查看全部活动 | 展示当前 Case 审计记录 |
| 人工证据、复核 | 表单校验、保存、审计、失败保留输入 |
| 筛选、上下页 | 浏览全部证据 |
| 图形缩放、适应、复位、节点详情 | 操作当前 Case 图形；节点双击或 Enter 查看原始记录 |
| 报告创建、三种导出、历史下载 | 下载冻结快照，显示 API 错误 |
| 选择进程 | 禁用，显示未配置隔离 worker |
| Playbook 新建、编辑、删除 | 保存文本定义；不执行内容 |
| 配置保存、恢复模板 | schema / 修订校验；恢复前显示范围 |
| 主题、窄屏 Copilot、提示词、发送 | 切换主题 / 面板，调用本地规则接口 |

## 验证

- `python -m unittest discover -s tests -v`：19 项通过。
- `node tests/test_frontend.cjs`：Case 隔离、离线动作、进度一致、草稿、分页、图引用通过。
- `python smoke-test.py`：临时端口、临时目录冒烟通过。
- `node --check app.js` 和 Python 编译检查通过。
- 隔离浏览器实际运行登记、metadata_check、人工复核及三种 Blob 下载；报告格式为 json/html/md，复核进度为 100%。
- 23 条证据显示为两页，第二页三条；翻页草稿保留。切换空 Case 后证据和图节点均为零。
- 人工证据表单、Playbook 三步骤表单保存成功。中央 scrollTop=500 时左右栏 top 均为 0。
- 窄屏面板可展开，页面没有横向溢出；浅色复核框白底深色字；控制台无 error/warning。
- 工作区 `data/state.json` 未写入验收数据。
- 浏览器真实拖动节点成功，节点坐标从 x=30 变为约 x=310，SVG 路径随之更新。
- 本地 8787 服务已重启加载新接口，`/api/settings` 返回 `runtime=not_implemented`。状态文件前后 SHA-256 均为 `7695422D5560ECA497BF48427F0043D6006F9E82BB2B3F26FF456569C7C1AABA`。

## 尚未实施，不应视为已完成

11.9 仅完成第一阶段配置管理。Provider 协议请求/响应适配、实际连接测试、模型发现、流式输出、子 Agent 调度/取消/权限继承、MCP 工具发现及隔离执行、Skill 安装解析仍未实现。这些接口明确返回 501，启用未实现运行时的配置返回 400，不能将保存草案理解为接入成功。

目前配置以完整 JSON 编辑，尚无逐项可视化表单、用户覆盖与未来模板升级的自动合并迁移。图布局仅在页面会话内保留；任意重叠拖动下的自动避障以及极大量节点性能还需专门完善和验收。
