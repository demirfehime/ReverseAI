# 网页 APK 接入验证

验证时间：2026-09-22 至 2026-09-23（Asia/Shanghai）。

- Python 全量回归：49 项通过，包含新上传校验、任务预算、单任务限制、取消进程及丢弃迟到结果、Case 及产物隔离测试。
- Node 前端及图路由检查通过；前端用例验证 APK 启动路由、模型及预算传参、运行中取消按钮、结果转义和证据链接。
- 本机 8787 服务已重启为 0.5，health 模式为 `metadata-and-apk-static`。

实际链路验证使用已下载的公开 ItsOnFire APK，样本 SHA-256 为 `eb6e69930b39a97ea4fa8a3c50d54ab7f13ea1223fa9bd2bedca0bbd000b0d92`。浏览器自动化文件选择器受到工具路径限制；真实文件通过同一 `/api/samples/apk` HTTP 接口上传，随后从网页控制器启动任务并核对页面展示。

Case：`case_8b4275f743444edc`（APK 网页接入验证）。任务：`job_3970bf739271419294f0cac774c50933`。使用已配置 Provider，模型为 `gpt-5.6-sol`，本次仅设 1 次调用作为接入验证。

JADX 产生部分反编译结果，页面明确提示错误及代码不完整风险；模型调用成功返回 `read` 操作，生成 `e0004`。共保存 4 条待复核证据。网页显示预算耗尽、未得出答案，最终结果下载返回 HTTP 200，`sample_executed=false`。这证明上传、反编译、真实 API 调用、证据写入、结束状态与下载链路可用，**不代表模型已自动解题**。

记录位于 `data/web-apk/job_3970bf739271419294f0cac774c50933/`，可在网页“APK 网页接入验证”项目查看。参考答案未发送至模型，服务默认模型配置未被本次任务改写。
